server:
  port: 8080

spring:
  application:
    name: sjtu-hire-backend
  datasource:
    url: jdbc:mysql://mysql.sqlpub.com:3306/sjtuhire?serverTimeZone=UTC;
    username: huangyixin
    password: IaWxUZH68Y01ZTQH
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          auto: validate
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
      naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl